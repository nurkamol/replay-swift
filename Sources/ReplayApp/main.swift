import ReplayCore

// Placeholder entry point; the app target is scaffolded in docs/PORTING-MAP.md.
@main
struct ReplayApp {
    static func main() {
        print("Replay native — core only for now. See docs/PORTING-MAP.md.")
    }
}
